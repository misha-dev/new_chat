import { Modal } from '@/shared/components/Modal/Modal';

export const CreateChatModal = ({ isOpen, onClose }) => {
  return (
    <Modal isOpen={isOpen} onClose={onClose}>
      <Modal.Header>
        <h2>Создание чата</h2>
      </Modal.Header>
      <Modal.Body>laksdjkdaskjl</Modal.Body>
    </Modal>
  );
};
